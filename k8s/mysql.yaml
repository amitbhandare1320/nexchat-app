apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: nexchat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "nexchat1234"
        - name: MYSQL_DATABASE
          value: "nexchat"
        - name: MYSQL_USER
          value: "nexchat"
        - name: MYSQL_PASSWORD
          value: "nexchat1234"
        ports:
        - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: nexchat
spec:
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306